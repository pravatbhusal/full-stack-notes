<?php
echo("Hello World"); //you need to use semi-colons, or it won't run!

//note that errors for line numbers will usually be on the line that has the end tag (since that's where php ends)
?> 
<!--HTML OUTSIDE of the PHP tags (because PHP is server-side, so it cannot go with the client)-->