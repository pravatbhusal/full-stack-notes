/* Exponentiation Operator
the exponentiation operator allows us to use exponents easily.
we don't need to use Math.pow(num1, num2) anymore
*/
let a = 7 ** 12; //this is 7^12
alert(a == Math.pow(7,12)); //returns true

//we can also use it as operation assignment
let b = 8;
b **= 12; //this is 8^12